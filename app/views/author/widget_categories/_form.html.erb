<% category = @author_widget_category %>
<%= form_for(category) do |f| %>

    <%= render_notification(category) %>
    <%= render_text_field(f, :name_index) %>
    <%= render_text_field(f, :name_value) %>
    <%= render_submit(f) %>

<% end %>
