<ul class="admin-navigation inner">
  <% @author_site_types.each do |author_site_type| %>
      <li>
        <%= link_to author_site_type.name.humanize, edit_author_site_type_path(author_site_type) %>
        <h3><%= author_site_type.get_sites(current_user).length %></h3>

        <div>
          <div>
            <ul class="menu arrow_box">
              <li><%= link_to t('edit'), edit_author_site_type_path(author_site_type) %></li>
              <li><%= link_to_destroy(author_site_type, author_site_type.name) %></li>
            </ul>
          </div>
        </div>
      </li>
  <% end %>
</ul>