<%= link_to t('login_in_link'), new_session_path(resource_name), class: 'btn btn-info' unless is_sessions? %>
<%= link_to t('sign_up_link'), new_registration_path(resource_name), class: 'btn btn-default' if is_sign_up_link? %>
<%= link_to t('forgot_password_link'), new_password_path(resource_name), class: 'btn btn-default' if is_forgot_password_link? %>
<%= link_to t('new_confirmation_link'), new_confirmation_path(resource_name), class: 'btn btn-warning' if is_new_confirmation_link? %>
<%= link_to t('new_unlock_link'), new_unlock_path(resource_name), class: 'btn btn-warning' if is_new_unlock_link? %>
