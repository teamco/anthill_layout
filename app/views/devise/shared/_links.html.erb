<%= button_link t('login_in_link'), new_session_path(resource_name), 'info',
    'fa-sign-in-alt', is_sessions?, false %>
<%= button_link t('sign_up_link'), new_registration_path(resource_name),
    'default', 'fa-user-plus', is_sign_up_link? %>
<%= button_link t('forgot_password_link'), new_password_path(resource_name),
    'warning', 'fa-memory', is_forgot_password_link? %>
<%#= button_link t('new_confirmation_link'),
    new_confirmation_path(resource_name), 'warning', 'fa-clipboard-check',
    is_new_confirmation_link? %>
<%#= button_link t('new_unlock_link'),
    new_unlock_path(resource_name), 'warning', 'fa-unlock-alt',
    is_new_unlock_link? %>
